---
title: Submit form response
description: Submit a completed form response
---

## Request

### Mutation

```graphql
mutation SubmitFormResponse($input: SubmitFormResponseInput!) {
  submitFormResponse(input: $input) {
    activity {
      id
      status
    }
  }
}
```

### Variables

```json
{
  "input": {
    "activity_id": "{{FORM_ACTIVITY_ID}}",
    "response": [
      {
        "question_id": "{{HEIGHT_QUESTION_ID}}",
        "value": "183"
      },
      {
        "question_id": "{{WEIGHT_QUESTION_ID}}",
        "value": "95"
      }
    ]
  }
}
```

### Specifying the user

The optional `X-User-ID` header can be used to specify which user is submitting the form response. When this header is present, Awell Orchestration will log a subactivity indicating that this specific user filled in the form.

```json
{
  "X-User-ID": "{{USER_ID}}"
}
```

The following input fields are available:

<div className="table-wrapper">
  <table>
    <thead>
      <tr>
        <th>Field</th>
        <th>Required</th>
        <th>Description</th>
        <th>Type</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>activity_id</td>
        <td>Yes</td>
        <td>the id of the form activity</td>
        <td>string</td>
      </tr>
      <tr>
        <td>response</td>
        <td>Yes</td>
        <td>the responses to questions in the form</td>
        <td>array of response objects</td>
      </tr>
      <tr>
        <td>response.question_id</td>
        <td>Yes, for each response object</td>
        <td>the id of the question for this response</td>
        <td>string</td>
      </tr>
      <tr>
        <td>response.value</td>
        <td>Yes, for each response object</td>
        <td>
          the value of the response, corresponding to the question's data point
          value type
        </td>
        <td>string</td>
      </tr>
    </tbody>
  </table>
</div>

## How to use

<HowToUse
  stories={[
    {
      title: 'Display a full form on a page',
      url: '/coming-soon',
      imageUrl:
        'https://res.cloudinary.com/da7x4rzl4/image/upload/v1658307742/Developer%20portal/full-form.png',
      badges: ['Custom integration', 'Forms'],
      description:
        'Learn how to display a form on a webpage and send the response back to the Awell API.',
    },
    {
      title: 'Display a form question-by-question (conversational)',
      url: '/awell-orchestration/docs/use-cases/custom-integration/stories/form/conversational-forms',
      imageUrl:
        'https://res.cloudinary.com/da7x4rzl4/image/upload/v1658309064/Developer%20portal/conversational-form.png',
      badges: ['Custom integration', 'Forms'],
      description:
        'Learn how to display a form in a conversational interface (question-by-question) and send the response back to the Awell API.',
    },
  ]}
/>
