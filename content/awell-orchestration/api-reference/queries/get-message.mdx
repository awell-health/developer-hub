---
title: Get message
description: Get the body and attachments of a message activity
---

This request allows us to fetch the details of a message activity.

## Request

### Query

```graphql
query GetMessage($id: String!) {
  message(id: $id) {
    message {
      id
      body
      format
      subject
      attachments {
        id
        name
        url
        type
      }
    }
  }
}
```

### Variables

An example `id` for a message is "9rjfb9qE4pEY".

```json
{
  "id": "{{MESSAGE_ID}}"
}
```

## How to use

<HowToUse
  stories={[
    {
      title: 'Send a notification when a new message is activated',
      url: '/coming-soon',
      badges: ['Custom integration', 'Notifications', 'Messages'],
      description:
        'Learn how to send notifications to your clients when there is a new message to read in their care flow.',
    },
    {
      title: 'Show a message to a client',
      url: '/coming-soon',
      badges: ['Custom integration', 'Messages'],
      description:
        'Learn how to display a message with subject, content, and attachments to your clients. Additionally, let the Awell API known that the message was read.',
    },
  ]}
/>
