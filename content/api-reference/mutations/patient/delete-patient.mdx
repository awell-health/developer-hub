---
title: Delete patient
description: Delete a patient record
---

## Request

### Query

```graphql
mutation deletePatient($input: DeletePatientInput!) {
  deletePatient(input: $input) {
    success
  }
}
```

### Variables

```json
{
  "input": {
    "patient_id": "{{PATIENT_ID}}"
  }
}
```

This mutation expects only one input variable:

<div className="table-wrapper">
  <table>
    <thead>
      <tr>
        <th>Field</th>
        <th>Required</th>
        <th>Description</th>
        <th>Type</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>patient_id</td>
        <td>Yes</td>
        <td>the id of the patient to be deleted</td>
        <td>string</td>
      </tr>
    </tbody>
  </table>
</div>
