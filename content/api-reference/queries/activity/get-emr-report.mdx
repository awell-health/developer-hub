---
title: Get EMR Report Details
description: Get the body of an EMR report activity
---

This request allows us to fetch the details of an EMR report activity.

## Request

### Query

```graphql
query GetEmrReport($id: String!) {
  emrReport(id: $id) {
    report {
      id
      message_html
      metadata {
        id
        label
        value
      }
    }
  }
}
```

### Variables

```json
{
  "id": "{{EMR_REPORT_ID}}"
}
```

This query expects only one input variable:

<div className="table-wrapper">
  <table>
    <thead>
      <tr>
        <th>Field</th>
        <th>Required</th>
        <th>Description</th>
        <th>Type</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>id</td>
        <td>Yes</td>
        <td>the id of the EMR report activity</td>
        <td>string</td>
      </tr>
    </tbody>
  </table>
</div>
